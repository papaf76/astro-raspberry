# Configurazione kstars

kstarsrc va in 
. ~/.config/kstarsrc per installazioni standard
. ~/.var/app/org.kde.kstars/config/ per flatpak

il contenuto dell'archivio local_share_kstars va in
. ~/.local/share/kstars per installazioni standard
. ~/.var/app/org.kde.kstars/data/kstars per flatpak
